# Output Contract

## Pack layout

```text
pack/
  diagram_manifest.yaml
  manifest_report.json
  diagram_pack_report.json
  <diagram-id>/
```

## Per diagram layout

```text
<diagram-id>/
  diagram_spec.md
  diagram_lock.yaml
  source.*
  semantic.svg or render_unavailable
  visual.svg or visual_failed
  preview.png
  preview_review.yaml
  delivery.png                 # required when raster_delivery is declared
  delivery_render_report.json  # required when raster_delivery is declared
  delivery_report.json         # generated by build_check_report.py
  embed.md                     # optional publication-adapter output
  check_report.json
```

`lock_report.json`, `semantic_report.json`, `visual_report.json`, `preview_report.json`, `preview_review_report.json`, optional `delivery_report.json`, and `check_report.json` must be generated by `build_check_report.py`. Do not hand-author pass statuses. The reports record source/semantic/visual/preview/delivery hashes, viewpoint and notation validation, type-native semantic-quality metrics, pack identity and diagram treatment validation, actual shape/stroke signatures, layout-plan and complexity/readability validation, geometry coverage, crossings, non-endpoint node intrusions, routing family, endpoint alignment, route modes, bend counts, direct-path blockers, diagonal permission, actual/direct detour ratios, v6 route-group coverage and metadata, independent-direct composition, shared-corridor coordinates, orbit curvature, long edges, delivery-scale typography, text containment, collisions, contrast, semantic drift, preview dimensions, raster pixel density, the review-to-preview-and-visual hash binding, preview-review contract compatibility with the diagram lock, and whether the visual geometry changed.

## Optional publication adapter

The core technical-diagram contract ends with validated visual artifacts and diagram/pack reports. Run `build_pack_report.py` to assemble the pack-level report without creating publication files.

When the user requests a Markdown publication adapter, `build_embed_blocks.py` may create `embed.md`. It embeds `visual.svg` first and links to `preview.png` as the consumer-size review artifact. When `raster_delivery` is declared, it also links to `delivery.png` as the high-density publication artifact. If `visual.svg` is missing, use `semantic.svg`. If both are unavailable, link to `render_unavailable` and `check_report.json` so the user sees the exact failure reason. The adapter is platform-neutral and must not change diagram semantics, geometry, styling, or quality rules.

The pack-level report exposes type, viewpoint, layout, notation, treatment, pack-identity, and actual geometry-signature counts. It must fail when contract v3 semantic diversity rules fail, a v4+ pack collapses distinct technical types into one unexplained rounded-card signature, a v5 diagram leaves treatment hierarchy or composition unexecuted, the manifest disagrees with a generated diagram lock, or any generated diagram has a missing, invalid, or failed check report.
