# Output Contract

## Pack layout

```text
pack/
  diagram_manifest.yaml
  manifest_report.json
  diagram_pack_report.json
  <diagram-id>/
```

## Per diagram layout

```text
<diagram-id>/
  diagram_spec.md
  diagram_lock.yaml
  source.*
  semantic.svg or render_unavailable
  visual.svg or visual_failed
  embed.md
  check_report.json
```

`lock_report.json`, `semantic_report.json`, `visual_report.json`, and `check_report.json` must be generated by `build_check_report.py`. Do not hand-author pass statuses. The reports record source/semantic/visual hashes, layout-plan and complexity validation, geometry coverage, crossings, non-endpoint node intrusions, long edges, semantic drift, and whether the visual geometry changed.

## Embed block

`embed.md` embeds `visual.svg` first. If `visual.svg` is missing, use `semantic.svg`. If both are unavailable, link to `render_unavailable` and `check_report.json` so the user sees the exact failure reason.

The pack-level report must fail when the manifest disagrees with a generated diagram lock or any generated diagram has a missing, invalid, or failed check report.
