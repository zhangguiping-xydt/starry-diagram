# Component

Generate when source material names modules, services, packages, or dependencies. Prefer Graphviz and strong visual enhancement.

Choose `dependency-map` for directed dependencies, `layered-system` for tiered components, and `bounded-context-map` for package or service ownership. Rank nodes by dependency depth before routing. Isolate cycles rather than allowing a force-directed hairball.
